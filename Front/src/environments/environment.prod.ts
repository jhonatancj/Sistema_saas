export const environment = {
  production: true,
  // TODO: reemplazar por la URL real del backend en producción antes de
  // desplegar — depende del dominio que se decida (ver
  // docker-compose.prod.yml + .env.production.example en la raíz del repo).
  apiBaseUrl: 'https://api.example.com',
};
